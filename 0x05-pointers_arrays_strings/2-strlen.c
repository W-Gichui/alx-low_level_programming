#include "main.h"

/**
 * _strlen - returns the length of a string
 * Return: length of a string
 * @s: string to be tested
 */

int _strlen(char *s)
{
	int i;

	int c;

	c = *s
	for (i = 0; c[i] != '\0'; i++)
	{
	}
	return (i);
}
